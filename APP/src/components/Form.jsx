import React from 'react';
import { InputAddText } from '../assest/styles/Input';

const Form = (props) => {
    return (
        <form>
            <InputAddText />
        </form>
    );
};

export default Form;